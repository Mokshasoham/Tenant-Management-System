/** Footer is drawn by the shared renderer on every page. This export satisfies the section library. */
export function renderFooter(_doc, _model, _def) {
    // no-op — footer is applied by SectionRenderer.drawFooter
}
